#include <stdio.h>
#include<conio.h>

//Compiler version gcc  6.3.0

int main()
{
  printf("\n Size for Integer Data type = %d",sizeof(int));
  
  printf("\n Size for float Data type = %d",sizeof(float));
  
  printf("\n Size for char Data type = %d",sizeof(char));
  
  printf("\n Size for double Data type = %d",sizeof(double));
  
  return 0;
  getch();
} 