#include<stdio.h>
#include<conio.h>>

int main()
{
        int No = 0;

        for(No = 0 ;No < 128;No++)
        {
          printf("\n ASCII Table %d = %c",No,No);
        }

        getch();
        return 0;
}
